import sqlite3

conn = sqlite3.connect("database/data.db")

conn.execute("""
CREATE TABLE reservations(
id INTEGER PRIMARY KEY,
name TEXT,
phone TEXT,
date TEXT,
time TEXT,
people INTEGER
)
""")

conn.execute("""
CREATE TABLE orders(
id INTEGER PRIMARY KEY,
name TEXT,
item TEXT,
quantity INTEGER
)
""")

conn.commit()